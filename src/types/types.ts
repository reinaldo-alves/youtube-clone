export interface IVideos {
    thumb: string,
    avatar: string,
    title: string,
    channel: string,
    views: string,
    time: string,
    color: string,
    description?: string
}

export interface IShorts {
    thumb: string,
    title: string,
    views: string
}